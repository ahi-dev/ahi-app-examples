buildscript {
    ext.kotlin_version = '1.6.10'
    repositories {
        google()
        jcenter()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.1.3'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        mavenCentral()
        maven {
            url "https://jitpack.io"
        }
        maven {
            // AHI MultiScan - Maven - Dev
            url "s3://myfiziq-android-maven/development"
            credentials(AwsCredentials) {
                accessKey "AKIAJUN5LPGUVVMKLJIA"
                secretKey "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"
            }
            content {
                includeGroupByRegex "com\\.myfiziq.*"
            }
        }
        maven {
            // AHI MultiScan - Maven - Release
            name "publicRelease"
            url "s3://myfiziq-android-sdk/release"
            credentials(AwsCredentials) {
                accessKey "AKIASR46NPIHVCEL63MH"
                secretKey "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"
            }
        }
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
